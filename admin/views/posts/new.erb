<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:posts, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:posts, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :post, url(:posts, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'posts/form', :locals => { :f => f } %>
 <% end %>
</div>
