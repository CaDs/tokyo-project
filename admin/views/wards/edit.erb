<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:wards, :index) %></li>
      <li><%= link_to pat(:new), url(:wards, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:wards, :edit, :id => @ward.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:ward) %></h2>
    <div class="inner">
      <% form_for :ward, url(:wards, :update, :id => @ward.id), :method => :put, :class => :form do |f| %>
        <%= partial "wards/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
