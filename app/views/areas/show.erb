<div class="container">
  <div class="row-fluid">
    <% @visions.in_groups_of(3) do |group| %>
      <ul class="thumbnails">
        <% group.compact.each do |vision| %>
          <li class="span4">
            <%= partial('layouts/landing_block', :locals => {:vision => vision, :show_link => true})%>
          </li>
        <% end %>
      </ul>
    <% end %>
  </div>
</div>
